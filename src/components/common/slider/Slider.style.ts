import styled from '@emotion/styled';

export const SDescroption = styled.div`
  position: absolute;
  top: 50%;
  left: 15%;
  transform: translate(-15%, -50%);

  color: white;
  text-align: left;
`